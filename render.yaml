services:
  - type: web
    name: carrier-power-system-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
      - key: PORT
        value: 8000
